name: Build and Test

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:  
  build-and-test:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '15.3'
    - name: Logs
      run: |
        /usr/bin/xcodebuild -version
        xcrun swift -version
    - name: Install Dependencies
      run: |
        ./.scripts/installBrewDependencyIfNeeded.sh "xcbeautify"
        ./.scripts/installBrewDependencyIfNeeded.sh "xcodegen"
        ./.scripts/installBrewDependencyIfNeeded.sh "swiftlint"
        ./.scripts/installBrewDependencyIfNeeded.sh "swiftgen"
    - name: Build and Test
      run: |
        make -f .ci-cd/build_and_test.mk
    - name: Run Danger
      if: github.event_name == 'pull_request'
      run: |
        bundle install
        bundle exec danger
      env:
        DANGER_GITHUB_API_TOKEN: ${{ secrets.DANGER_GITHUB_API_TOKEN }}
