# See: https://circleci.com/docs/configuration-reference

version: 2.1

jobs:
  test:
    macos:
      xcode: 14.2.0
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            make -f ../.ci-cd/build_and_test.mk

workflows:
  test:
    jobs:
      - test
